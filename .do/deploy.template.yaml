spec:
  name: portfolio
  services:
    - name: web
      git:
        branch: main
        repo_clone_url: https://github.com/kushakjafry/portfolio.git
      build_command: npm run build:ssr
      run_command: npm run serve:ssr
